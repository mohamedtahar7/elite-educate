import GetStartedCard from "@/components/main/GetStartedCard";
import React from "react";

const page = () => {
  return (
    <main className="md:pt-24 pb-12 flex items-center justify-center pt-20">
      <GetStartedCard />
    </main>
  );
};

export default page;
